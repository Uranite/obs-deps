try to compile libvpx with clang
if not possible, turn libvpx mod into a patch

PGO svt-av1

see if a patch for libsrt is possible

turn lame mod into a patch

for ffmpeg:
sed -i 's/if test "`$cc_type" = "clang"; then/if true; then/' configure
sed -i 's/test "`$cc_type" != "`$ld_type" && die "LTO requires same compiler and linker"/true/' configure
    --enable-lto="thin" \
    --extra-cflags="-flto=thin /clang:-O3" \

see if it's possible to compile modify luajit to compile with clang

turn speexdsp mod into a patch

check zlib unistd patch

try msvc v145

fix cache hmm maybe we can make it a workflow_dispatch thing

done:
check if setting CMAKE_RC_COMPILER is still necessary or not
check qt6 mod
check libvpx llvm-ar